# STEP66 - GitHub Actions quality gate fix

Date: 2026-04-22
Scope: GitHub Actions runner `ops/gates_run_ci.ps1`, no HA runtime deploy.

## FACT

- GitHub mail notifications are generated by the remote workflow `.github/workflows/quality-gates.yml`.
- That workflow runs `ops/gates_run_ci.ps1`, not the local `ops/gates_run.ps1`.
- Previous fixes made local connector-first gates pass, but did not change the CI runner.
- `ops/gates_run_ci.ps1` still enumerated all Git-tracked YAML files without the scoped runtime exclusions now used locally.

## IPOTESI

- Confidenza alta: GitHub notifications were caused by the CI runner path, not by local `ops/validate.ps1`.
- Confidenza alta: scoping CI YAML lint to operational paths avoids false failures from vendored/tooling YAML.
- Confidenza alta: adding the Lovelace gate to CI aligns remote quality checks with local validation.

## DECISIONE

- Update `ops/gates_run_ci.ps1` to:
  - resolve repo root with a script-relative fallback
  - enumerate YAML through Git when available
  - fall back to `rg` if needed
  - apply the same scoped exclusions used by local gates
  - include `ops/gate_lovelace_dashboards.ps1`
- Keep `.github/workflows/quality-gates.yml` unchanged.

## Verification

- Local `ops/validate.ps1`: already PASS after STEP65.
- Local `ops/gates_run_ci.ps1`: PASS, output ended with `ALL GATES PASSED`.
- Remote verification requires the next GitHub Actions run on push.
